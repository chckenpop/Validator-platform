## Backend Purpose

This backend stores and validates atomic learning concepts.

Key principles:
- Concepts are immutable via versioning
- Content is never overwritten
- Domain logic lives outside HTTP
- Frontend is a client, not an authority
